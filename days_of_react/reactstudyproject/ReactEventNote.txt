# e.preventDefault(); --> 태그의 기본적인 동작을 못하게 만듬

# Event가 호출될 때 쓰이는 this의 값은 아무것도 가르키고 있지 않음
# function(){} .bind(this) 와 같은 형식으로 this 값을 넘길 수 있음
# this.setState({}) 와 같은 형태로 사용할 수 있음 <-- state의 값이 바뀔 떄 사용

